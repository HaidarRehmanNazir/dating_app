import 'package:flutter/material.dart';

class HashLogo extends StatelessWidget {
  const HashLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      'Hash Dating',
      style: TextStyle(
        fontSize: 35,
        fontWeight: FontWeight.bold,
        color: Colors.white,
        fontFamily: 'FontMain',
      ),
    );
  }
}
